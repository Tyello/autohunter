# RUN — 004-fipe-on-demand-lookup

Loop contract: máx. 2 escalações/etapa, 3 reprovações/etapa, 20 iterações totais.

| # | Evento |
|---|---|
| 1 | spec.md finalizado e revisado (análise de consistência OK) |
| 2 | Nota: subagentes spec-kit indisponíveis neste harness — executando etapas mecanicamente conforme fallback da skill |
| 3 | Etapa 1 (settings) — OK (feita junto com spec 003 Etapa 1, mesmos arquivos). Validação: `py -c "..."` → OK |
| 4 | Etapa 2 [sensível] (modelo FipeLookupRequest + migration) — OK. Divergência vs. spec (Realidade ≠ spec): a spec previa `down_revision = "b2c4d6e8f0a1"` para a NOVA migration, mas esse ID já é usado por uma migration existente (`b2c4d6e8f0a1_system_logs_component_message_index.py`, head real da linha FIPE/system_logs). Resolvido (auto, sem humano): nova migration recebeu revision id próprio `d4e6f8a0b2c4`, com `down_revision = "b2c4d6e8f0a1"` (ou seja, encadeada A PARTIR do head citado, que era o head correto — só o nome da nova revisão que a spec sugeriu colidia). `app/models/fipe_lookup_request.py` criado conforme contrato (TimestampMixin, CHECK status, index status+created_at, FK wishlists.id ondelete=CASCADE). Registrado em `app/models/__init__.py` (mesmo padrão de `FipeUpdateRun`). `pytest tests/test_fipe_lookup_request_model.py -q` → 2/2 OK. `alembic heads` confirma `d4e6f8a0b2c4` como head alcançável a partir de `b2c4d6e8f0a1`. |
| 5 | Etapa 3 (enqueue + pseudo-listing) — OK. Criado `app/services/fipe_on_demand_lookup_service.py` com `enqueue_fipe_lookup_for_wishlist` (kill switch + dedupe pending + try/except/rollback nunca propaga) e `_build_pseudo_listing` (PREM-01 split marca/modelo por token; PREM-02 gte-preferido-a-lte para ano, `None` se nenhum filtro `field="year"`). `pytest tests/test_fipe_on_demand_lookup_service.py -q -k "enqueue or pseudo_listing"` → 8/8 OK. |
| 6 | Etapa 4 [sensível] (processamento da fila + refresh direcionado) — OK. Duas divergências vs. spec (Realidade ≠ spec), resolvidas sem escalar a humano: (1) `resolve_listing_to_fipe_candidates` NÃO aceita `reference_month=None` diretamente (chama `normalize_fipe_month` que levanta `ValueError` em `None`) — resolvido chamando `_ensure_month(db, None)` primeiro no próprio serviço e passando o mês resolvido adiante, preservando a intenção da spec (mês mais recente) sem alterar o resolver. (2) `best_candidate` retornado pelo resolver é um **dict de score** (`catalog_entry_id`, `confidence_score`, etc.), não o objeto `FipeCatalogEntry` — não tem `.brand_code`/`.model_code`/`.updated_at` diretamente. Resolvido buscando a entry real via `db.query(FipeCatalogEntry).filter(id == uuid.UUID(best[\"catalog_entry_id\"]))` para obter os campos usados no frescor/refresh. Refresh direcionado reaproveita `client.get_latest_reference_table`/`get_model_years`/`get_price` (nunca `get_brands`/`get_models`) e `upsert_fipe_catalog_entries` via `normalize_external_fipe_row` (mesmo parser testado da spec 001, evita duplicar lógica de parsing de preço BRL). Erros de API tratados por `attempts`/`last_error`/transição `pending`↔`failed` conforme `fipe_lookup_max_attempts`; try/except externo em `process_pending_fipe_lookups` isola falha inesperada de 1 pedido sem abortar o lote (teste dedicado). `pytest tests/test_fipe_on_demand_lookup_service.py -q -k process` → 7/7 OK (incl. `test_process_retries_then_fails_after_max_attempts`, adicionado em correção posterior); suíte completa do módulo → 14/14 OK. |
| 7 | Etapa 5 (job do scheduler) — OK. Criado `app/scheduler/fipe_lookup_job.py::job_process_fipe_lookups()` seguindo exatamente o padrão de `operational_data_cleanup_job.py`: checa `is_shutdown_requested()`, abre `SessionLocal()`, chama `process_pending_fipe_lookups(db)`, loga via `system_logs_service.log(db, "info"/"error", "fipe_lookup", ..., {**res, "ms": dt_ms})`, comita em ambos os ramos (sucesso e falha), nunca propaga exceção. `pytest tests/test_fipe_lookup_job.py -q` → 2/2 OK. |
| 8 | Etapa 6 [sensível] (registro no scheduler) — OK. `app/scheduler/run.py`: adicionado executor dedicado `"fipe_lookup": ThreadPoolExecutor(2)` no dict `executors=` do `BackgroundScheduler`, e `add_job(job_process_fipe_lookups, "interval", seconds=settings.fipe_lookup_poll_interval_s, id="fipe_lookup_poll", replace_existing=True, max_instances=1, coalesce=True, executor="fipe_lookup")`, logo após o registro do job mensal `monthly_fipe_update`. Isola o lookup on-demand do executor `"fipe"` do crawler mensal (não competem por thread). Autocorreção: primeira versão usava `id="fipe_lookup_process_pending"` e `ThreadPoolExecutor(1)`; releitura do spec.md confirmou contrato exato (`id="fipe_lookup_poll"`, pool size 2) e foi corrigido antes de prosseguir. `pytest tests/test_scheduler_fipe_lookup_registration.py -q` → 2/2 OK; `pytest tests/test_scheduler_fipe_registration.py -q` (regressão) → 2/2 OK. |
| 9 | Etapa 7 (integração na criação de wishlist, teste 18) — OK. Divergência vs. spec (Realidade ≠ spec): spec citava `tests/test_wishlists_service.py`, mas esse arquivo não existe — os 4 pontos de chamada (`add_wishlist`, `create_wishlist_with_filters`, `add_wishlist_with_initial_summary`, `create_wishlist_with_filters_and_initial_summary`) já são cobertos por `tests/test_wishlist_initial_run.py` e `tests/test_guided_wishlist_creation.py`. Resolvido (auto, sem humano): os 4 sub-testes do teste 18 foram adicionados em `tests/test_wishlist_initial_run.py` (mesmo arquivo que já importa e testa essas 4 funções), nomeados com sufixo `fipe_lookup_enqueued` para permitir `-k fipe_lookup`. `app/services/wishlists_service.py`: import `enqueue_fipe_lookup_for_wishlist` + chamada adicionada logo após cada uma das 4 chamadas a `trigger_initial_run_for_wishlist` (linhas ~712, 1171, 1201, 1253), preservando o branch `enqueue_initial_run=False` de `add_wishlist` sem lookup (mesma decisão já implícita na spec: lookup só dispara quando a busca inicial também dispara). `pytest tests/test_wishlist_initial_run.py -q -k fipe_lookup` → 4/4 OK; `pytest tests/test_wishlist_initial_run.py tests/test_guided_wishlist_creation.py -q` (regressão completa) → 51/51 OK. |
| 10 | Etapa 8 (regressão completa) — OK. Divergência vs. spec: comando citava `tests/test_wishlists_service.py` (não existe, ver evento 9); substituído por `tests/test_wishlist_initial_run.py tests/test_guided_wishlist_creation.py`. `pytest tests/test_fipe_on_demand_lookup_service.py tests/test_fipe_lookup_job.py tests/test_scheduler_fipe_lookup_registration.py tests/test_fipe_lookup_request_model.py tests/test_wishlist_initial_run.py tests/test_guided_wishlist_creation.py -q` → 100% verde. Suíte completa do repositório (`pytest tests/ -q`) → exit code 0, sem falhas. |
| 11 | Verificação final (spec-verifier simulado) — APROVADO. Checados os 4 objetivos e as 6 premissas contra evidência arquivo:linha: enqueue sem chamada de rede (`app/services/fipe_on_demand_lookup_service.py:52-71`), poller com executor dedicado `"fipe_lookup"` reusando candidato fresco ou fazendo refresh direcionado (`fipe_on_demand_lookup_service.py:149-197`, `app/scheduler/run.py` registro do job), falha nunca derruba o fluxo e respeita `fipe_lookup_max_attempts` deixando o veículo elegível para o sync mensal (`fipe_on_demand_lookup_service.py:179-197`, `process_pending_fipe_lookups:200-232`), sync mensal (spec 003) intocado nesta spec. 4 call sites de `wishlists_service.py` confirmados com `enqueue_fipe_lookup_for_wishlist` ao lado de `trigger_initial_run_for_wishlist`. Spec 003 (FIPE monthly job perf) e Spec 004 (on-demand lookup) ambas 100% completas. Suíte completa do repositório verde. Nenhuma lacuna encontrada; specs 003 e 004 encerradas. |
