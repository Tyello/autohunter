# RUN — Spec 021 (OLX: extração de ano/km + badge de recência)

- 2026-09-04: Spec criada a partir do diagnóstico read-only (sessão anterior). Tier: T2. Nenhuma etapa executada ainda.
- 2026-09-04: Etapa 1 executada — nenhum teste com asserção de shape fechado encontrado; extras são passthrough em finalize_listings (contract.py:105-117); pode prosseguir para Etapa 2.
- 2026-09-04: Etapa 2 corrigida (DVG-001) — evidência de validação: comando ".venv/Scripts/python.exe -c \"from app.scrapers.olx import _extract_year_from_title, _extract_mileage_from_title; print(_extract_year_from_title('Honda Fit 2007 1.4 Flex')); print(_extract_mileage_from_title('Honda Fit 2007 45.000 km'))\"" → saída: "2007" / "45.000 km".
- 2026-09-04: Etapa 3 executada — `_items_to_dicts` (app/scrapers/olx.py) agora inclui `"year": _extract_year_from_title(it.title)` e `"km": _extract_mileage_from_title(it.title)`. Revisor reprovou minor (DVG-001, evidência de validação incompleta no relatório do executor) — código confirmado correto pelo próprio revisor via git diff; orquestrador reexecutou a validação diretamente: comando `.venv/Scripts/python.exe -m pytest tests/test_scraper_parsing.py -k olx -v` → "1 passed, 4 deselected in 7.65s"; comando `.venv/Scripts/python.exe -c "from app.scrapers.olx import OlxItem, _items_to_dicts; from decimal import Decimal; item = OlxItem(external_id='1', title='Honda Fit 2007 1.4 Flex 45.000 km', url='http://x', thumbnail_url=None, price=Decimal('20000')); d = _items_to_dicts([item])[0]; print(d.get('year')); print(d.get('km'))"` → saída: "2007" / "45.000 km". DVG-001 resolvido, etapa aprovada.
- 2026-09-04: Etapa 4 executada — `build_recency_badge` (app/notifications/telegram_formatter.py) troca "🆕 Novo" por "🆕 Anúncio novo no feed" no branch de fallback (hours<2, baseado em created_at); 2 asserções atualizadas em tests/test_telegram_formatter_vnext.py. Validação: `pytest -k "badge or recency or telegram_formatter" -v` → 60 passed. Revisor reprovou major (DVG-001) alegando vazamento de escopo para app/scrapers/olx.py — FALSO POSITIVO: nenhum commit foi feito entre etapas, então `git diff` mostra o acumulado das etapas 2+3+4; `git diff --stat` confirmado pelo orquestrador mostra exatamente os 3 arquivos esperados (telegram_formatter.py, test_telegram_formatter_vnext.py já revisados nesta etapa, e olx.py já revisado/aprovado nas etapas 2-3). Etapa 4 considerada aprovada pelo orquestrador.
- 2026-09-04: Etapa 5 executada e APROVADA pelo spec-reviewer — 2 testes novos em tests/test_scraper_parsing.py cobrindo REQ-001/002/003/005. `pytest tests/test_scraper_parsing.py -v` → 7 passed.
- 2026-09-04: Spec 021 fechada — T2 sem etapas [sensível], spec-verifier não obrigatório. Todas as 5 etapas aprovadas (etapa 3 e 4 tiveram reprovações minor/major de revisor por questões de evidência/baseline de diff, ambas resolvidas pelo orquestrador sem reabrir código). Nenhum commit git feito — mudanças ficam no working tree para o usuário revisar/commitar.
- 2026-09-05: Commit b085acc + deploy em produção (Raspberry Pi 192.168.0.146, /opt/autohunter via `sudo -u autohunter git pull`, restart de autohunter-api e autohunter-scheduler).
- 2026-09-05: REGRESSÃO EM PRODUÇÃO reportada pelo usuário ("fipe ainda nao ta funcionando"). Causa raiz encontrada por leitura de código (não coberta pela spec original): `app/repositories/car_listings_repo.py::insert_ignore_duplicates_return_ids` (linhas 423-437) NÃO passa por `app/sources/normalize.py` — para o schema atual (colunas `year`/`mileage_km` são `Integer`, ver app/models/car_listing.py:58-59), o valor bruto do dict do scraper é atribuído direto em `l["mileage_km"]`/mantido em `l["year"]` sem nenhuma coerção numérica. `_extract_year_from_title`/`_extract_mileage_from_title` (etapas 2-3) retornavam STRINGS (ex: "2007", "45.000 km"), incompatíveis com colunas Integer via asyncpg — isso provavelmente quebrava o bulk insert/upsert para os registros OLX, explicando por que year/km/FIPE continuaram vazios após o deploy.
- 2026-09-05: FIX-001 aplicado (fora do escopo original da spec, mesma área de código) — `_extract_year_from_title` e `_extract_mileage_from_title` (app/scrapers/olx.py) agora retornam `Optional[int]` em vez de `Optional[str]` (conversão via `int()` / `re.sub(r"\D", "", ...)`), eliminando a incompatibilidade de tipo com as colunas Integer. Testes atualizados: tests/test_scraper_parsing.py (`test_olx_extracts_year_and_mileage_from_title`, `test_olx_missing_year_or_mileage_returns_none`) agora esperam int. Validação: `pytest tests/test_scraper_parsing.py -v` → 7 passed; `pytest tests/ -k "olx or car_listings_repo or scraper"` → 104 passed, 2023 deselected.
