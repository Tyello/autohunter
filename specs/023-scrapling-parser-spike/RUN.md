# RUN — Spec 023 (Spike: parser Scrapling vs BeautifulSoup no OLX)

- 2026-09-25: Spec criada. Tier: T2 (5pts). Pré-condição (bug OLX de year/mileage) confirmada fechada via specs/021-olx-year-mileage-extraction (commits b085acc, cb0e9bd, verificado em produção 2026-09-05). Nenhuma etapa executada ainda.
- 2026-09-25: Etapas 1-6 executadas e aprovadas (etapa 3 revisada pelo spec-reviewer: APROVADO). Etapa 7 (consolidação do entregável) e etapa 8 (confirmação de escopo read-only) executadas diretamente pelo orquestrador, sem delegar ao Haiku, porque exigiam uma decisão de julgamento não coberta pela spec: o ganho de (c) scrapling variou de ~15-17x (fixtures de detalhe) a ~1.0-1.1x (fixture de busca/RSC, caminho quente de maior volume) — ambiguidade prevista na condição ESCALA SE da etapa 7. Resolução: veredito NO-GO, já que o critério GO (≥5x) precisa valer para a operação de maior peso real (busca/RSC), e ali o gargalo é regex sobre JSON bruto, não o parser de árvore — nenhuma troca de parser ataca esse gargalo. Entregável final em docs/spikes/scrapling-parser-spike.md.
- 2026-09-25: Etapa 8 — git status --short confirma escopo: apenas docs/spikes/scrapling-parser-spike.md, scripts/spikes/bench_scrapling_parser.py, specs/023-scrapling-parser-spike/ novos; git diff --stat vazio para app/, migrations/, requirements*.txt, config/, deploy/. `.venv-spike/` se autoignora (venv do Python 3.13 cria .gitignore próprio com "*" dentro do diretório) — não foi necessário alterar o .gitignore do repo (PREM-04 desnecessária). Spec 023 fechada. Todos os REQ-001 a REQ-008 atendidos.
- 2026-09-25: Usuário pediu para implementar "o que temos certeza de melhora". Investigação adicional revelou que scrapling adiciona 6 dependências novas (incluindo orjson, binário compilado) mesmo instalando só o parser — custo desproporcional para o único ganho comprovado sem risco de ADR-0001 (thumbnail de detalhe, capado em ~3 chamadas/run). Implementado em vez disso, sem dependências novas: (1) troca de parser BS4 html.parser→lxml em app/scrapers/olx.py:252 (_extract_olx_detail_thumbnail, frio) e :363 (_extract_next_data_json, quente) — lxml já é dependência do projeto, equivalência confirmada nas 5 fixtures antes da mudança; (2) short-circuit em _extract_next_data_json (retorna None antes de parsear se "__NEXT_DATA__" não está na string) — estritamente equivalente ao comportamento anterior, já que o regex de fallback também exige essa substring. NÃO alterado app/scrapers/olx.py:757 (_fallback_parse_from_cards) — sem cobertura de teste com card HTML real, e é o ponto que alimenta external_id/dedupe (risco ADR-0001). Validação: pytest tests/ -k olx → 25 passed. Diff único em app/scrapers/olx.py, revisado manualmente (escopo mínimo confirmado).
- 2026-09-25: Validação em hardware real solicitada pelo usuário — acesso via SSH a pi@192.168.0.146 (Raspberry Pi 4 Model B, 4GB, produção). Benchmark rodado em venv isolada em /tmp/spike-bench (fora do checkout de produção em /opt/autohunter, sem tocar app/ nem reiniciar serviços). Confirma padrão da spike: scrapling ~23x mais rápido em fixtures de detalhe, mas só ~1.45x na fixture de busca/RSC (caminho quente) — ainda não atinge o critério de 5x, reforça veredito NO-GO. Short-circuit isolado medido em ~248x de ganho na operação que evita (0.778ms → 0.0031ms/chamada, 100 execuções, fixture real sem __NEXT_DATA__). Correção de precisão no entregável: a fixture search_rsc_price_nodes.html tem ~15KB, não "~2MB" como escrito antes (erro de interpretação do pico de memória do tracemalloc); corrigido em docs/spikes/scrapling-parser-spike.md. Diretório /tmp/spike-bench na Pi ainda não foi limpo — é escrata isolada, não afeta produção.
