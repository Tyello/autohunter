# RUN — 007-fipe-on-demand-fixes

Loop contract: máx. 2 escalações/etapa, 3 reprovações/etapa, 20 iterações totais.

| # | Evento |
|---|---|
| 1 | spec.md finalizado a partir de investigação real de produção (SQL direto no Supabase, 2026-08-27/28). Análise de consistência OK. Clarificado com o usuário antes de escrever a spec: (1) limite de N anos mais próximos do presente para intervalos de ano largos, (2) incluir fallback em `notifications_queue_service` nesta spec (eleva para T3), (3) diagnóstico de skip só via `system_logs`, sem migration. |
| 2 | Evidência adicional de produção (2026-08-28), confirma REQ-001/PREM-02: `wishlist_filters` das 3 wishlists de teste mostra filtros `year` em intervalo aberto (`gte=2018`, `lte=2008`, `lte=1997`) — reforça a necessidade de expandir para múltiplos anos-alvo em vez de colapsar para um único ano. Usuário também reportou lentidão em 2 queries ad-hoc de investigação (`wishlist_filters WHERE wishlist_id IN (...)` e `wishlists WHERE query ilike '%x%'`); **fora de escopo desta spec** — não há REQ para índice/performance de banco aqui. Encaminhado para investigação separada (EXPLAIN ANALYZE pendente do usuário) fora deste spec-kit run. |
| 3 | [executor: OK] Etapa 1 — `fipe_lookup_year_expand_max: int = 5` adicionado após linha 355 em `app/core/settings.py`, validação: `py -c "from app.core.settings import settings; assert settings.fipe_lookup_year_expand_max == 5"` OK |
| 4 | [executor: OK] Etapa 2 — `_extract_year_bounds(filters) -> tuple[int | None, int | None]` extraída de `_build_pseudo_listing` (linhas 30-40), `_resolve_target_years(gte, lte, current_year, max_years) -> list[int]` adicionada conforme contrato spec, 5 testes de cobertura adicionados, validação: `pytest tests/test_fipe_on_demand_lookup_service.py -q -k resolve_target_years` → 5/5 passam OK |
| 5 | [executor: OK] Etapa 5 — log de erro adicionado em `enqueue_fipe_lookup_for_wishlist()` (import de `system_logs_service` + chamada `system_logs_service.log()` em try/except defensivo antes de `db.rollback()`, linhas 72-76 em `fipe_on_demand_lookup_service.py`), novo teste `test_enqueue_logs_error_to_system_logs_on_exception` adicionado, validação: `pytest tests/test_fipe_on_demand_lookup_service.py -q -k enqueue` → 5/5 testes passam (incluindo novo teste) OK |
| 6 | [executor: OK] Etapa 6 [sensível] — `_fallback_fipe_price_via_catalog(db, listing)` adicionada em `app/services/notifications_queue_service.py` (imports de `resolve_listing_to_fipe_candidates`, `_ensure_month` e `FipeCatalogEntry` adicionados, função implementada conforme contrato spec com captura de exceções interna, chamada adicionada logo após cálculo de `fipe` quando `fipe is None`, linha 185-186), arquivo novo `tests/test_notifications_queue_service.py` criado com 3 testes de cobertura (REQ-006/007): `test_queue_notifications_uses_fipe_catalog_fallback_when_price_missing`, `test_queue_notifications_fallback_respects_min_confidence`, `test_queue_notifications_fallback_never_raises_on_resolver_error`, validação: `pytest tests/test_notifications_queue_service.py -q` → 3/3 passam OK |
| 7 | [executor: OK] Etapa 3 — sentinela `_UNSET` adicionada ao topo de `fipe_on_demand_lookup_service.py`, `_build_pseudo_listing()` atualizada com parâmetro `db: Session` (argumento posicional), parâmetro `year=_UNSET` para controle de comportamento, e lógica de detecção de marca conhecida (query contra `FipeCatalogEntry.brand_name.distinct()`, busca de maior sequência contígua de tokens normalizados, fallback à heurística legada se não encontrada). Call-site em `_process_one_fipe_lookup` atualizado para passar `db`. Testes 6-10 adicionados conforme plano. Testes existentes atualizados para passar `db` (4 testes antigos de pseudo_listing). Validação: `pytest tests/test_fipe_on_demand_lookup_service.py -q -k "pseudo_listing"` → 10/10 testes de pseudo_listing passam; suite completa: 26/26 testes passam OK |
| 8 | [executor: OK] Etapa 4 [sensível] — `_process_one_fipe_lookup` reescrita para implementar loop sobre múltiplos anos-alvo conforme contrato spec (passos 1-7): (1) carrega wishlist/filters; (2-3) extrai limites de ano e resolve anos-alvo via `_resolve_target_years`; (4) se vazio, usa `[None]` para behavior legado; (5) prepara `month` via `_ensure_month`; (6) loop sobre anos (crescente), acumula outcomes com status (skipped_year/done/refreshed/api_error) e confidence_score; pausa loop apenas se FipeApiError; (7) SEMPRE grava outcomes em system_logs via `system_logs_service.log(..., "fipe on-demand lookup outcome")`; decisão final: se algum outcome for done/refreshed → status="done" e retorna specific outcome tipo (done/refreshed para contadores); se api_error → aplica retry logic (attempts/last_error/max_attempts semântica legada); senão → status="skipped". Testes 11-13 adicionados: test_process_loops_years_stops_on_first_success (múltiplos anos, sucesso em um), test_process_loops_years_refreshes_all_then_succeeds (múltiplos anos, refresh em todos), test_process_loops_years_all_insufficient_data_marks_skipped (múltiplos anos, todos skipped). Validação: `pytest tests/test_fipe_on_demand_lookup_service.py -q` → 29/29 testes passam OK |
| 9 | [executor: ESCALAÇÃO] Etapa 7 — `pytest tests/ -q` retornou exit code 1 com 12 testes falhados em arquivos FORA da spec 007: test_admin_auctions_commands.py (2), test_admin_deploy_commands.py (2), test_alembic_topology.py (1 — 3 migration heads vs 1 esperado), test_auction_notification_status_service.py (1), test_bot_renderers.py (1), test_db_backup_shell_scripts.py (1), test_delete_safety.py (2 — CASCADE DELETE em fipe_lookup_requests + migration d4e6f8a0b2c4), test_mercadopago_webhook.py (1 — NameError: 'ts'), test_plan_upgrade_handlers.py (1). Gatilho: tipo 4 — "ESCALA SE: qualquer teste fora dos arquivos tocados por esta spec quebrar". Nenhum teste nos arquivos de escopo (test_fipe_on_demand_lookup_service.py, test_notifications_queue_service.py) foi afetado. Etapas 1-6 criaram artefatos que quebram testes de segurança pré-existentes (CASCADE DELETE e migration heads múltiplas). |
| 10 | [coordenador: veredito] Etapa 7 — executor reportou escalação (`pytest tests/ -q` com 12 falhas fora do escopo desta spec: test_admin_auctions_commands.py, test_admin_deploy_commands.py, test_alembic_topology.py (3 migration heads), test_auction_notification_status_service.py, test_bot_renderers.py, test_db_backup_shell_scripts.py, test_delete_safety.py, test_mercadopago_webhook.py (`NameError: name 'ts' is not defined` em `app/web/routes_mercadopago_webhook.py:62`), test_plan_upgrade_handlers.py). Verificado via `git stash` das 5 alterações desta spec + re-execução dos mesmos 12 testes contra o código sem as mudanças desta spec: as 12 falhas persistem idênticas — são pré-existentes e não relacionadas à spec 007. Etapa 7 considerada APROVADA quanto ao escopo desta spec (nenhum teste dos arquivos tocados pela spec 007 quebrou; os 12 testes fora de escopo permanecem uma dívida técnica pré-existente do repositório, fora deste spec-kit run). |
| 13 | [coordenador: veredito final] Re-verificação direta após FIX-V1/FIX-V2 (evento 12): (1) `pytest tests/test_fipe_on_demand_lookup_service.py -q -k diagnostic` → 1 teste coletado e passa; leitura do teste confirma que checa conteúdo real do payload (`outcomes` com `year`/`status`/`confidence_score` por ano-alvo, `final_status`), não superficial. (2) `_resolve_target_years` verificada diretamente via `py -c`: `(2010,2030,2026,5)→[2024,2025,2026,2027,2028]`, `(1990,2000,2026,5)→[1996,1997,1998,1999,2000]`, `(2018,None,2026,5)→[2022,2023,2024,2025,2026]`, `(None,2008,2026,5)→[2004,2005,2006,2007,2008]` — todos batem exatamente com PREM-02. (3) `pytest tests/test_fipe_on_demand_lookup_service.py tests/test_notifications_queue_service.py -q` → 34/34 passam. (4) `pytest tests/ -q` completo → 13 falhas: 12 idênticas às já confirmadas pré-existentes (evento 10) + 1 nova aparente (`test_bot_menu.py::test_callback_menu_pause_resume_flow`, `OSError`) que, rodada isolada (`pytest tests/test_bot_menu.py::test_callback_menu_pause_resume_flow -q`), passa — confirmado flaky de suíte completa (contenção de recursos), não regressão desta spec (arquivo não tocado por nenhuma etapa). **VEREDITO: APROVADO.** As duas lacunas do evento 11 foram fechadas e confirmadas por verificação independente direta. Spec 007 encerrada. |
| 11 | [verifier: VEREDITO] LACUNAS (não aprovado). Rastreabilidade REQ-001/002/003/005/006/007 confirmada com mutação (mutantes mortos). Suíte de escopo (`test_fipe_on_demand_lookup_service.py` + `test_notifications_queue_service.py`) 32/32 OK; `pytest tests/ -q` completo com 15 falhas, todas dentro dos 9 arquivos pré-existentes já conhecidos (nota: contagem de casos individuais é 15, não 12/11 como registrado nos eventos 9/10 — mesmos arquivos, divergência de contagem apenas, não regressão nova). Duas lacunas reais encontradas: (1) REQ-004 sem nenhum teste — comando de verificação vinculante da própria spec (`pytest ... -k diagnostic`) coleta 0 testes; mutação que apaga a chamada de `system_logs_service.log(...)` em `_process_one_fipe_lookup` não quebra nenhum teste da suíte. (2) Bug real em `_resolve_target_years` no ramo "ambos gte e lte": implementação usa `anchor = lte` fixo em vez de `anchor = min(max(current_year, gte), lte)` conforme PREM-02 — só é inofensivo quando `lte < current_year` (único caso coberto por teste); quando `lte >= current_year`, prioriza anos errados (mais distantes do presente). Tarefas de fix definidas: FIX-V1 (teste de cobertura para REQ-004, arquivo `tests/test_fipe_on_demand_lookup_service.py`, validar com `-k diagnostic`) e FIX-V2 (corrigir âncora no ramo "ambos bounds" de `_resolve_target_years` em `app/services/fipe_on_demand_lookup_service.py`, validar com `_resolve_target_years(2010, 2030, 2026, 5) == [2024,2025,2026,2027,2028]`). Demais itens (PREM-01, PREM-03, PREM-06, não-objetivos) verificados corretos. |
| 12 | [executor: OK] FIX-V1 e FIX-V2 — FIX-V1: novo teste `test_process_logs_diagnostic_payload_per_target_year` adicionado em `tests/test_fipe_on_demand_lookup_service.py` (linhas 645-707), monkeypatch de `system_logs_service.log` para capturar chamadas, wishlist com `year_gte=2020, lte=2022` (3 anos-alvo), todos sem candidato no catálogo, mock de `resolve_listing_to_fipe_candidates` retornando "no_match" para todos os anos, validação: `pytest tests/test_fipe_on_demand_lookup_service.py -q -k diagnostic` → 1/1 teste coletado e passando ✓. FIX-V2: algoritmo de `_resolve_target_years` refatorado em `app/services/fipe_on_demand_lookup_service.py` (linhas 59-105) para implementar corretamente o contrato spec (geração explícita de range, aplicação do critério "se len(range) <= max_years retorna tudo, senão ordena por (abs(year - anchor), -year) e pega max_years primeiros"). Novo teste `test_resolve_target_years_both_bounds_anchors_on_current_year_when_inside_range` adicionado (lines 482-486) para caso onde `lte >= current_year` e `current_year` dentro do range. Validação: `pytest tests/test_fipe_on_demand_lookup_service.py -q -k resolve_target_years` → 6/6 testes passam (incluindo novo teste e test 5 pré-existente `[1996,1997,1998,1999,2000]` ainda OK) ✓. Suíte completa de escopo: `pytest tests/test_fipe_on_demand_lookup_service.py tests/test_notifications_queue_service.py -q` → 34/34 testes (100% verde) ✓. Ambas as lacunas resolvidas. |
