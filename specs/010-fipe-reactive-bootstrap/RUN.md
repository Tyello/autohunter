# RUN — Spec 010: Bootstrap FIPE reativo por anúncio

Tier: T2 — 7pts (arquivos=2, decisões=2, risco=2, novidade=1, verif=1)

## Eventos

- [2026-08-28] Spec escrita e aprovada internamente. 4 etapas definidas (1=schema[sensível], 2=processing reativo, 3=trigger/enqueue, 4=fechamento).
- [2026-08-28] Etapa 1 [sensível] concluída pelo spec-executor: migração `e2f3a4b5c6d7` (down_revision=d4e6f8a0b2c4) adiciona `listing_make`/`listing_model`/`target_year` nullable a `fipe_lookup_requests`; model atualizado; setting `fipe_lookup_reactive_cooldown_days=7` adicionada. `alembic heads` confirma 3 heads (e2f3a4b5c6d7 substituindo d4e6f8a0b2c4, mais os 2 pré-existentes intactos). Testes: `test_fipe_lookup_request_model.py` (2 passed), `test_fipe_on_demand_lookup_service.py` (53 passed). Enviado para revisão sênior (etapa sensível).
- [2026-08-28] Revisão sênior etapa 1: REPROVADO (major) — os 3 arquivos do escopo estavam corretos, mas o executor deixou uma alteração não declarada e fora de escopo em `.env.example` (`FIPE_MONTHLY_UPDATE_ENABLED=false`→`true`), contradizendo o comentário ao lado ("continua desligado até ser setado manualmente em produção"). Corrigido diretamente (revertido para `false`) sem reabrir o executor, por ser trivial e fora do código da etapa. Etapa 1 considerada fechada após a correção.
- [2026-08-28] Etapa 2 (não sensível) concluída pelo spec-executor: `_process_one_reactive_fipe_lookup` adicionada em `fipe_on_demand_lookup_service.py`, roteamento em `process_pending_fipe_lookups` via `request.listing_make is not None`. 3 testes novos. `py -m pytest tests/test_fipe_on_demand_lookup_service.py -q` → 56/57 passam (contagem variou entre execuções do relatório, sem falhas reportadas). Enviado para revisão mecânica (spec-reviewer).
- [2026-08-29] Revisão mecânica etapa 2: REPROVADO (major) por "vazamento de escopo" para `app/core/settings.py`, `app/models/fipe_lookup_request.py` e a migração — porém isso é falso positivo do revisor: nenhuma dessas etapas commita entre si (sem pedido do usuário), então o `git diff` do working tree acumula etapa 1 + etapa 2 juntas. Confirmado via `git log`/`git status`: nenhum commit novo foi criado pela etapa 2, os únicos commits existentes são de specs 008/009 anteriores a esta spec. O conteúdo real e específico da etapa 2 (`_process_one_reactive_fipe_lookup`, roteamento, 3 testes, reaproveitamento de `_resolve_fipe_brand_and_models`/`_bootstrap_fipe_catalog_entries_for_year`, `_build_pseudo_listing`/`_resolve_target_years` intactos, 56 testes passando) foi validado como correto pelo revisor. Etapa 2 considerada APROVADA (achado de escopo descartado como falso positivo, achados técnicos todos positivos).
- [2026-08-29] Etapa 3 (não sensível) concluída pelo spec-executor: `_enqueue_reactive_fipe_lookup` adicionada em `notifications_queue_service.py`, chamada logo após `_fallback_fipe_price_via_catalog` retornar `None`; dedup/cooldown via query contra `FipeLookupRequest`. 7 testes novos + 3 pré-existentes = 10 passando (`py -m pytest tests/test_notifications_queue_service.py -q`). Enviado para revisão mecânica.
- [2026-08-29] Revisão mecânica etapa 3: APROVADO. Confirmado escopo restrito a `notifications_queue_service.py` + testes, dedup/cooldown correto, timezone handling defensivo, `_fallback_fipe_price_via_catalog` intacta, 10/10 testes passando.
